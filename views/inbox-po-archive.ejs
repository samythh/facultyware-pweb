<%- include('partials/header') %>
<%- include('partials/sidebar') %>

<main id="content">
  <%- include('partials/navbar') %>

  <div class="p-4 md:p-6 xl:p-12">
    <div class="max-w-screen-lg mx-auto flex flex-col gap-6">
      <header class="flex flex-wrap items-end justify-between gap-4">
        <div class="flex flex-col gap-2">
          <h1 class="text-2xl font-bold tracking-tight sm:text-3xl"><%= title %></h1>
          <p class="text-muted-foreground">
            Riwayat keputusan persetujuan Purchase Order (gerbang kedua, sadar-harga).
          </p>
        </div>
        <%- include('partials/sort-control', { action: '/approval/po/archive', current: (typeof sort !== 'undefined' ? sort : ''), opts: (typeof sortOptions !== 'undefined' ? sortOptions : []), keep: {} }) %>
      </header>

      <div class="card card--table overflow-hidden">
        <div class="overflow-x-auto">
          <table class="w-full text-left border-collapse text-sm">
            <thead>
              <tr class="bg-muted/50 border-b">
                <th class="p-4 font-semibold text-muted-foreground w-[180px]">No PO</th>
                <th class="p-4 font-semibold text-muted-foreground">Supplier</th>
                <th class="p-4 font-semibold text-muted-foreground text-right w-[160px]">Total</th>
                <th class="p-4 font-semibold text-muted-foreground w-[160px]">Penyetuju</th>
                <th class="p-4 font-semibold text-muted-foreground w-[150px]">Tgl Keputusan</th>
                <th class="p-4 font-semibold text-muted-foreground w-[150px]">Status</th>
                <th class="p-4 font-semibold text-muted-foreground w-[100px] text-right">Aksi</th>
              </tr>
            </thead>
            <tbody class="divide-y">
              <% if (purchases && purchases.length > 0) { %>
                <% purchases.forEach(function (po) { %>
                  <tr class="hover:bg-muted/30 transition-colors cursor-pointer" onclick="window.location.href='/purchase/<%= po.id %>'">
                    <td class="p-4 font-mono font-semibold text-primary"><%= po.purchase_number %></td>
                    <td class="p-4 font-medium"><%= po.supplier || '-' %></td>
                    <td class="p-4 text-right font-mono font-semibold text-primary">Rp <%= Number(po.total || 0).toLocaleString('id-ID') %></td>
                    <td class="p-4 text-muted-foreground"><%= po.approver_name || '-' %></td>
                    <td class="p-4 text-muted-foreground"><%= po.approved_at ? fmtDate(po.approved_at) : '-' %></td>
                    <td class="p-4"><%- include('partials/status-badge', { status: po.status }) %></td>
                    <td class="p-4 text-right">
                      <a href="/purchase/<%= po.id %>" class="btn-xs-outline inline-flex items-center" onclick="event.stopPropagation()">Detail</a>
                    </td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr>
                  <td colspan="7" class="p-8 text-center text-muted-foreground">
                    Belum ada riwayat keputusan Purchase Order.
                  </td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>

        <div class="flex items-center justify-between border-t px-4 py-4 text-sm text-muted-foreground">
          <span>Menampilkan <%= purchases ? purchases.length : 0 %> keputusan PO</span>
        </div>
      </div>
    </div>
  </div>
</main>

<%- include('partials/footer') %>
